# name: lint-format

# on: [pull_request, push]

# permissions:
#   contents: write
#   pull-requests: write

# jobs:
#   lint:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Log in to GitHub Container Registry
#         uses: docker/login-action@v2
#         with:
#           registry: ghcr.io
#           username: ${{ github.actor }}
#           token: ${{ secrets.GITHUB_TOKEN }}

#       - name: Pull Docker image from GHCR
#         run: |
#           docker pull ghcr.io/YOUR_GITHUB_USERNAME/YOUR_REPO_NAME/my-linter-image:latest

#       - name: Run Linter
#         run: |
#           docker run -v $(pwd):/workspace -w /workspace ghcr.io/YOUR_GITHUB_USERNAME/YOUR_REPO_NAME/my-linter-image:latest lint

#   build:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Check out code
#         uses: actions/checkout@v2

#       - name: Log in to GitHub Container Registry
#         uses: docker/login-action@v2
#         with:
#           registry: ghcr.io
#           username: ${{ github.actor }}
#           token: ${{ secrets.GITHUB_TOKEN }}

#       - name: Pull Docker image from GHCR
#         run: |
#           docker pull ghcr.io/YOUR_GITHUB_USERNAME/YOUR_REPO_NAME/my-linter-image:latest

#       - name: Build Project
#         run: |
#           docker run -v $(pwd):/workspace -w /workspace ghcr.io/YOUR_GITHUB_USERNAME/YOUR_REPO_NAME/my-linter-image:latest build